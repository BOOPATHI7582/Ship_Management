# Public values only - never put secrets here.
# Backend runs locally on :8080. Works when the browser is on the same
# machine as the backend (dev/demo from this computer).
VITE_API_BASE_URL=http://localhost:8080/api
