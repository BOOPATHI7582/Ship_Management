VITE_API_BASE_URL=http://localhost:8080/api
# Google sign-in: set to your OAuth 2.0 Web client ID (public) to enable the button.
# Get it from Google Cloud Console > APIs & Services > Credentials > OAuth client ID (Web).
VITE_GOOGLE_CLIENT_ID=
