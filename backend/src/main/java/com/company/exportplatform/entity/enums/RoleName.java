package com.company.exportplatform.entity.enums;

/**
 * Initial user roles per spec. Future roles (SUPER_ADMIN, SALES, OPERATIONS,
 * FINANCE, ACCOUNTANT) will be added when required.
 */
public enum RoleName {
    CLIENT,
    ADMIN,
    SHIP_MANAGER
}
